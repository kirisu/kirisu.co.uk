language: node_js
  - "lts/*"
install:
  - yarn
script:
  - yarn export
deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: out
  keep-history: true
  on:
    branch: master
